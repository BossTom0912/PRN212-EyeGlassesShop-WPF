<Window x:Class="WpfApp_CLassesShop.CheckoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_CLassesShop"
        mc:Ignorable="d"
        Title="Xác nhận thanh toán" Height="550" Width="500" 
        WindowStartupLocation="CenterScreen" Background="#F4F6F9" FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Border Background="White" CornerRadius="15" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="#B0BEC5" BlurRadius="20" ShadowDepth="3" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="THÔNG TIN GIAO HÀNG" FontSize="22" FontWeight="Bold" 
                           Margin="0,0,0,25" HorizontalAlignment="Center" Foreground="#E65100"/>

                <TextBlock Text="Tên người nhận (*):" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtReceiverName" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                <TextBlock Text="Số điện thoại (*):" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtPhone" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                <TextBlock Text="Địa chỉ nhận hàng (*):" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}" 
                         Height="80" VerticalContentAlignment="Top" Padding="10"
                         TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,25"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnCancel" Grid.Column="0" Content="Hủy bỏ" Height="45" 
                            Background="#EDF2F7" Foreground="#4A5568" Margin="0,0,10,0"
                            Style="{StaticResource ModernButton}" Click="btnCancel_Click"/>

                    <Button x:Name="btnConfirm" Grid.Column="1" Content="XÁC NHẬN ĐẶT HÀNG" Height="45"
                            Background="#2E86C1" Foreground="White"
                            Style="{StaticResource ModernButton}" Click="btnConfirm_Click"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>